# Arc brief — <arc name>

One brief per campaign; attempts are recorded as they run. This is the
whole ceremony for explore-mode work — do not add per-attempt plans,
handoffs, or manifests.

## Objective

<What this arc is trying to learn or produce, in one or two sentences.>

## Sandbox

<Where this arc is allowed to write. Everything outside is protected.>

## Protected boundary

<What must not change: canonical content, accepted evidence, live config.
Name paths. If a step needs to cross this line, that step is promote-mode
and needs its own authority.>

## Phase-change signal

<The observable result that ends the arc — either freezing a candidate
for validation, or concluding the approach failed. Without this, arcs
never end; they dissipate.>

## Attempt log

<Append per attempt: number, date, command/config, result, evidence ref.
A code or input change is a NEW attempt, never a rewrite of an old one.
Failures stay — they are the map of where not to dig again.>
